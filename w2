using System;
namespace Work
{
    public class Program
    {
        public static void Main(string[] args)
        {
			string cont;
			cont = "sim";

			do
			{
			
			Console.WriteLine("digite o numero de seu interesse: ");
            string a = Console.ReadLine();
            Console.WriteLine("digite outro numero: ");
            string b = Console.ReadLine();


            if (double.TryParse(a, out double dec1))
            {}
            else
            {
                Console.WriteLine("digite um numero valido");
				continue;
            }
            if (double.TryParse(b, out double  dec2))
            {}
            else
            {
                Console.WriteLine("digite um numero valido");
				continue;
            }


            Console.WriteLine("escolha a operacao desejada: ");
            string opr = Console.ReadLine();

            double resultado = 0;


            if (opr == "+")
            {
                resultado = dec1 + dec2;
            }
            else if(opr == "-")
            {
                resultado = dec1 - dec2;
            }
            else if(opr == "*")
            {
                resultado = dec1 * dec2;
            }
            else if(opr == "/")
            {
				if (dec2 == 0)
				{
					Console.WriteLine("invalido, digite um numero diferente de 0");
					continue;
				}
                resultado = dec1 / dec2;
            }
            else
            {
                Console.WriteLine("digite uma operacao valida.");
				continue;
            }
			
			Console.WriteLine($"resultado da sua operacao e {resultado}");
			Console.WriteLine("deseja continuar? sim/nao");
				cont = Console.ReadLine();
			}	
			
			while (cont == "sim");
			
        }
    }
}
